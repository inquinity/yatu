# Unreleased

Notes for the next release. Written as changes land, not reconstructed at
release time — which is how the first two releases' notes were produced, from
memory, into a temporary file.

`bin/release.sh` refuses to publish if this file is empty, and uses it as both
the tag message and the GitHub release body.
